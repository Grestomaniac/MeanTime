<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="android.view.View"/>
        <import type="com.sillyapps.meantime.data.State"/>

        <variable
            name="viewmodel"
            type="com.sillyapps.meantime.ui.mainscreen.MainViewModel" />
    </data>


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/refreshDay"
            isActive="@{viewmodel.paused}"
            android:layout_width="0dp"

            android:layout_height="0dp"
            android:visibility="visible"

            app:layout_constraintBottom_toTopOf="@+id/buttonsContainer"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:visibility="visible">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/runningTasks"
                android:layout_width="match_parent"
                android:layout_height="match_parent"

                android:paddingBottom="8dp"
                android:clipToPadding="false"

                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                tools:listitem="@layout/item_main_screen_task" />

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/buttonsContainer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:background="@drawable/main_screen_navigation"
            android:paddingTop="16dp">

            <ImageButton
                android:id="@+id/play_button"
                style="@style/ActionButtonStyle"
                android:layout_width="@dimen/centerButtonSize"

                android:layout_height="@dimen/centerButtonSize"
                android:layout_marginBottom="16dp"
                android:onClick="@{() -> viewmodel.startButtonPressed()}"

                android:src="@drawable/ic_play"
                app:isInteractive="@{viewmodel.uiDayState != State.ACTIVE}"

                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="@+id/button_pause"

                app:layout_constraintStart_toStartOf="@+id/button_pause"
                tools:visibility="visible"/>

            <ImageButton
                android:id="@+id/button_pause"
                style="@style/ActionButtonStyle"
                android:layout_width="@dimen/centerButtonSize"

                android:layout_height="@dimen/centerButtonSize"

                android:layout_marginBottom="16dp"
                android:onClick="@{() -> viewmodel.pauseButtonPressed()}"

                android:src="@drawable/ic_pause"

                android:tint="@color/secondaryTextColor"

                app:isInteractive="@{viewmodel.uiDayState == State.ACTIVE}"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/addTemporalTaskButton"
                app:layout_constraintStart_toEndOf="@+id/button_stop"
                tools:visibility="invisible" />

            <ImageButton
                android:id="@+id/button_stop"
                style="@style/ActionButtonStyle"
                android:layout_width="@dimen/buttonSize"
                android:layout_height="@dimen/buttonSize"

                android:layout_margin="@dimen/normal_margin"
                android:onClick="@{() -> viewmodel.stopButtonPressed()}"
                android:src="@drawable/ic_stop_black_24dp"

                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/addTemporalTaskButton"
                style="@style/ActionButtonStyle"
                android:layout_width="wrap_content"

                android:layout_height="wrap_content"
                android:layout_margin="@dimen/normal_margin"
                android:src="@drawable/ic_add"
                app:fabSize="normal"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>